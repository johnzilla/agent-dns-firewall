---
phase: 03-quality-and-ship
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - QUAL-04

must_haves:
  truths:
    - "A developer reading only the README can understand what the library does, install it, and use it correctly"
    - "The README accurately reflects the actual exported API and types"
    - "The README clearly states what the library is NOT (not a DNS server, not a proxy)"
  artifacts:
    - path: "README.md"
      provides: "Complete library documentation"
      contains: "Quick Start"
  key_links:
    - from: "README.md"
      to: "src/index.ts"
      via: "import examples match actual exports"
      pattern: "createDomainFirewall|PRESET_STEVENBLACK_UNIFIED|PRESET_HAGEZI_LIGHT"
---

<objective>
Write a complete README for the library covering pitch, quick start, configuration reference, API reference, and scope boundaries.

Purpose: QUAL-04 requires documentation sufficient for a developer to adopt the library from the README alone. The current README is just a title.
Output: Complete README.md with all required sections.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-quality-and-ship/03-RESEARCH.md

@src/types.ts
@src/index.ts
@src/presets.ts
@src/firewall.ts
@package.json
</context>

<interfaces>
<!-- Exports from src/index.ts that the README must reference accurately -->

From src/types.ts:
```typescript
export interface BlockDecision {
  blocked: boolean;
  reason?: 'custom-deny' | 'blocklist';
  listId?: string;
}

export interface BlocklistSource {
  id: string;
  url: string;
  format: 'hosts' | 'domains';
}

export interface FirewallConfig {
  sources: BlocklistSource[];
  allow?: string[];
  deny?: string[];
  refreshMinutes?: number;
  log?: (level: 'warn' | 'error', message: string) => void;
}

export interface DomainFirewall {
  start(): Promise<void>;
  stop(): void;
  isDomainBlocked(domain: string): BlockDecision;
}
```

From src/presets.ts:
```typescript
export const PRESET_STEVENBLACK_UNIFIED: BlocklistSource;  // stevenblack-unified, hosts format
export const PRESET_HAGEZI_LIGHT: BlocklistSource;          // hagezi-light, domains format
```

From src/index.ts:
```typescript
export { createDomainFirewall } from './firewall.js';
export { PRESET_STEVENBLACK_UNIFIED, PRESET_HAGEZI_LIGHT } from './presets.js';
// Also exports: normalizeDomain, parseHostsFormat, parseDomainList, buildDomainIndex,
//   isDomainInIndex, sanitizeInput, isDomainBlocked, and all type interfaces
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write complete README</name>
  <files>README.md</files>
  <action>
Replace the current single-line README with a complete document. Use ESM `import` syntax only (package is `"type": "module"`). Note Node 18+ requirement (uses native `fetch`). Zero runtime dependencies.

Structure (from QUAL-04 and research):

1. **Title + pitch**: `# agent-dns-firewall` followed by 1-2 sentence pitch derived from PROJECT.md core value: "Before your agent calls fetch(), ask isDomainBlocked(hostname) and drop known-bad destinations -- no infrastructure required."

2. **What This Is / What This Isn't**: Two-column table or bullet lists. IS: in-process domain lookup, blocklist-aware, zero-dependency, works with any HTTP client. IS NOT: DNS server, DNS proxy, HTTP proxy, system-level blocker, URL/path filter. Draw from REQUIREMENTS.md Out of Scope section.

3. **Quick Start**: `npm install agent-dns-firewall` then a working 8-10 line TypeScript example using `createDomainFirewall` with `PRESET_STEVENBLACK_UNIFIED`. Show `await firewall.start()`, `firewall.isDomainBlocked()`, and `firewall.stop()`. Must match actual exports exactly.

4. **Configuration Reference**: Document `FirewallConfig` fields with types and descriptions. Document `BlocklistSource` shape. Document presets (`PRESET_STEVENBLACK_UNIFIED`, `PRESET_HAGEZI_LIGHT`). Show an advanced example combining multiple sources, allow/deny lists, and refreshMinutes.

5. **API Reference**: Document `createDomainFirewall(config)` return type (`DomainFirewall`), its methods (`start()`, `stop()`, `isDomainBlocked()`), and `BlockDecision` return shape.

6. **License**: MIT (matches package.json).

Keep the tone concise and technical. No badges, no contributor guide, no changelog section. This is a small focused library README.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const r = fs.readFileSync('README.md','utf8'); const sections = ['Quick Start','What This Is','Configuration','API','License']; const missing = sections.filter(s => !r.includes(s)); if(missing.length) { console.error('Missing sections:', missing); process.exit(1); } console.log('All required sections present');"</automated>
  </verify>
  <done>README.md contains all QUAL-04 required sections: pitch, quick start with working example, configuration reference, API reference, and "what this is / what this isn't" scope section. All import paths and type names match the actual exported API.</done>
</task>

</tasks>

<verification>
README.md exists with all required sections. Code examples use ESM import syntax and reference only actual exports from src/index.ts.
</verification>

<success_criteria>
- README contains pitch (1-2 sentences about the core value proposition)
- README contains "What This Is / What This Isn't" with scope boundaries
- README contains Quick Start with install command and working code example
- README contains Configuration Reference documenting FirewallConfig, BlocklistSource, presets
- README contains API Reference documenting createDomainFirewall, DomainFirewall methods, BlockDecision
- All code examples use `import` syntax (not `require`)
- All referenced exports actually exist in src/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-and-ship/03-02-SUMMARY.md`
</output>
