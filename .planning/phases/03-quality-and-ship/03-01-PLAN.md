---
phase: 03-quality-and-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/parse.test.ts
  - tests/match.test.ts
  - tests/decide.test.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "Hosts-format parser handles tabs, whitespace-only lines, trailing whitespace, and unusual input without errors"
    - "Suffix matching correctly rejects partial label overlaps and handles single-label index entries"
    - "Allow/deny precedence is correct for all pairwise combinations including allow+blocklist-no-deny and deny+blocklist-same-domain"
  artifacts:
    - path: "tests/parse.test.ts"
      provides: "QUAL-01 edge case tests"
      contains: "QUAL-01"
    - path: "tests/match.test.ts"
      provides: "QUAL-02 edge case tests"
      contains: "QUAL-02"
    - path: "tests/decide.test.ts"
      provides: "QUAL-03 combination tests"
      contains: "QUAL-03"
  key_links:
    - from: "tests/parse.test.ts"
      to: "src/parse.ts"
      via: "import parseHostsFormat, parseDomainList"
      pattern: "import.*parseHostsFormat.*parseDomainList"
    - from: "tests/match.test.ts"
      to: "src/match.ts"
      via: "import buildDomainIndex, isDomainInIndex"
      pattern: "import.*buildDomainIndex.*isDomainInIndex"
    - from: "tests/decide.test.ts"
      to: "src/decide.ts"
      via: "import isDomainBlocked"
      pattern: "import.*isDomainBlocked"
---

<objective>
Add targeted edge case tests to existing test files to close the gaps identified in research for QUAL-01, QUAL-02, and QUAL-03.

Purpose: The existing 100-test suite covers the core scenarios but is missing specific edge cases called out in the quality requirements -- tab separators, whitespace-only lines, label-boundary false positives, and pairwise precedence combinations.
Output: 3 updated test files with ~10 new test cases total, all passing.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-quality-and-ship/03-RESEARCH.md

@tests/parse.test.ts
@tests/match.test.ts
@tests/decide.test.ts
@src/parse.ts
@src/match.ts
@src/decide.ts
@src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add QUAL-01 parsing edge case tests</name>
  <files>tests/parse.test.ts</files>
  <behavior>
    - parseHostsFormat with tab separator (`0.0.0.0\tad.com`) returns `['ad.com']`
    - parseHostsFormat with whitespace-only line (`   \t  \n0.0.0.0 ad.com`) returns `['ad.com']`
    - parseHostsFormat with trailing whitespace (`0.0.0.0 ad.com   `) returns `['ad.com']`
    - parseDomainList with trailing whitespace (`  example.com  `) returns `['example.com']`
    - parseDomainList with whitespace-only line between domains returns only the real domains
  </behavior>
  <action>
Add a new `describe('parseHostsFormat edge cases (QUAL-01)')` block at the end of parse.test.ts with tests for: (1) tab-separated fields, (2) whitespace-only lines, (3) trailing whitespace after domain. Add a matching `describe('parseDomainList edge cases (QUAL-01)')` block for: (4) trailing whitespace, (5) whitespace-only lines. Follow the existing test pattern using `describe`/`it` with `expect().toEqual()`.
  </action>
  <verify>
    <automated>npx vitest run tests/parse.test.ts -x</automated>
  </verify>
  <done>All new QUAL-01 edge case tests pass alongside existing parse tests. Tab separators, whitespace-only lines, and trailing whitespace are explicitly verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add QUAL-02 matching and QUAL-03 precedence edge case tests</name>
  <files>tests/match.test.ts, tests/decide.test.ts</files>
  <behavior>
    - isDomainInIndex: single-label domain `com` in index matches `anything.com` (suffix walk)
    - isDomainInIndex: `ware.test` in index does NOT match `malware.test` (wrong label boundary)
    - isDomainBlocked: domain in allow + blocklist (no deny) returns `{ blocked: false }` -- allow wins
    - isDomainBlocked: subdomain of allowed domain in blocklist returns `{ blocked: true }` -- allow is exact match only
    - isDomainBlocked: domain in both deny and blocklist returns reason `custom-deny` (deny takes priority over blocklist)
    - isDomainBlocked: domain in second of two blocklists gets the second list's sourceId
  </behavior>
  <action>
In match.test.ts, add a `describe('isDomainInIndex edge cases (QUAL-02)')` block with 2 tests: (1) single-label domain in index (`com`) matching `anything.com`, (2) `ware.test` in index NOT matching `malware.test` (partial label overlap, wrong boundary).

In decide.test.ts, add a `describe('Precedence combinations (QUAL-03)')` block with 3-4 tests: (1) allow + blocklist with no deny -- domain allowed, subdomain blocked, (2) deny + blocklist same domain -- reason is `custom-deny` not `blocklist`, (3) domain in second of two blocklists gets second list's sourceId. Use `buildDomainIndex` for creating test index sets, matching the existing test patterns in the file.
  </action>
  <verify>
    <automated>npx vitest run tests/match.test.ts tests/decide.test.ts -x</automated>
  </verify>
  <done>All new QUAL-02 and QUAL-03 edge case tests pass. Label-boundary false positive is explicitly guarded. All pairwise precedence combinations have explicit test coverage.</done>
</task>

</tasks>

<verification>
npx vitest run
All 100+ tests pass (existing + new edge cases). No regressions.
</verification>

<success_criteria>
- All existing tests still pass (zero regressions)
- New tests cover every gap identified in 03-RESEARCH.md Test Gap Analysis
- QUAL-01: tab separators, whitespace-only lines, trailing whitespace tested
- QUAL-02: single-label index entry and wrong-label-boundary false positive tested
- QUAL-03: allow+blocklist, deny+blocklist priority, multi-list sourceId tested
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-and-ship/03-01-SUMMARY.md`
</output>
