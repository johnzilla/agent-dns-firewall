---
phase: 02-lifecycle-and-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/presets.ts
  - src/fetch.ts
  - tests/presets.test.ts
  - tests/fetch.test.ts
autonomous: true
requirements: [PARSE-03, RESL-01, CONF-01, CONF-02, CONF-03]

must_haves:
  truths:
    - "Preset constants provide correct URLs and format for StevenBlack and Hagezi lists"
    - "A single blocklist source can be fetched from a URL and parsed into a domain array"
    - "Multiple sources can be fetched in parallel with individual failure isolation"
    - "Failed fetches log warnings with source ID and error details, not throw"
    - "FirewallConfig accepts an optional log callback"
  artifacts:
    - path: "src/types.ts"
      provides: "FirewallConfig with log callback"
      contains: "log?"
    - path: "src/presets.ts"
      provides: "PRESET_STEVENBLACK_UNIFIED and PRESET_HAGEZI_LIGHT constants"
      exports: ["PRESET_STEVENBLACK_UNIFIED", "PRESET_HAGEZI_LIGHT"]
    - path: "src/fetch.ts"
      provides: "fetchSource and fetchAllSources functions"
      exports: ["fetchSource", "fetchAllSources"]
    - path: "tests/presets.test.ts"
      provides: "Preset shape and value tests"
    - path: "tests/fetch.test.ts"
      provides: "Fetch mocking tests for success, failure, and mixed scenarios"
  key_links:
    - from: "src/fetch.ts"
      to: "src/parse.ts"
      via: "import parseHostsFormat, parseDomainList"
      pattern: "import.*parse"
    - from: "src/fetch.ts"
      to: "src/match.ts"
      via: "import buildDomainIndex"
      pattern: "import.*buildDomainIndex"
    - from: "src/presets.ts"
      to: "src/types.ts"
      via: "import BlocklistSource type"
      pattern: "import.*BlocklistSource"
---

<objective>
Create the building blocks for Phase 2: preset configurations, HTTP fetching with error isolation, and the log callback type addition.

Purpose: These modules are consumed by the factory function (Plan 02). Presets provide zero-config blocklist sources. The fetch module bridges HTTP to the Phase 1 parsers. Error isolation ensures the "always start successfully" contract.

Output: src/presets.ts, src/fetch.ts, updated src/types.ts, and corresponding tests.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lifecycle-and-configuration/02-CONTEXT.md
@.planning/phases/02-lifecycle-and-configuration/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts from Phase 1 that this plan consumes. -->

From src/types.ts:
```typescript
export interface BlockDecision {
  blocked: boolean;
  reason?: 'custom-deny' | 'blocklist';
  listId?: string;
}

export interface BlocklistSource {
  id: string;
  url: string;
  format: 'hosts' | 'domains';
}

export interface FirewallConfig {
  sources: BlocklistSource[];
  allow?: string[];
  deny?: string[];
  refreshMinutes?: number;
}

export interface DomainFirewall {
  start(): Promise<void>;
  stop(): void;
  isDomainBlocked(domain: string): BlockDecision;
}
```

From src/parse.ts:
```typescript
export function parseHostsFormat(content: string): string[];
export function parseDomainList(content: string): string[];
```

From src/match.ts:
```typescript
export function buildDomainIndex(domains: string[]): Set<string>;
export function isDomainInIndex(domain: string, index: Set<string>): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types update and preset constants</name>
  <files>src/types.ts, src/presets.ts, tests/presets.test.ts</files>
  <behavior>
    - Test: FirewallConfig type accepts optional log callback of type (level: 'warn' | 'error', message: string) => void
    - Test: PRESET_STEVENBLACK_UNIFIED has id 'stevenblack-unified', format 'hosts', URL pointing to raw.githubusercontent.com StevenBlack hosts
    - Test: PRESET_HAGEZI_LIGHT has id 'hagezi-light', format 'domains', URL pointing to raw.githubusercontent.com hagezi light list
    - Test: Both presets satisfy the BlocklistSource interface (id, url, format fields present)
  </behavior>
  <action>
1. Add `log?: (level: 'warn' | 'error', message: string) => void` to the `FirewallConfig` interface in src/types.ts. Do NOT change any other interfaces.

2. Create src/presets.ts with two exported constants:
   - `PRESET_STEVENBLACK_UNIFIED`: `{ id: 'stevenblack-unified', url: 'https://raw.githubusercontent.com/StevenBlack/hosts/master/hosts', format: 'hosts' }`
   - `PRESET_HAGEZI_LIGHT`: `{ id: 'hagezi-light', url: 'https://raw.githubusercontent.com/hagezi/dns-blocklists/main/domains/light.txt', format: 'domains' }`
   - Import `BlocklistSource` type from `./types.js`

3. Create tests/presets.test.ts verifying each preset has correct id, url, format, and satisfies BlocklistSource shape.

TDD: Write tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npx vitest run tests/presets.test.ts -x</automated>
  </verify>
  <done>PRESET_STEVENBLACK_UNIFIED and PRESET_HAGEZI_LIGHT exported with correct shapes and URLs. FirewallConfig has log callback. All preset tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HTTP fetch module with error isolation</name>
  <files>src/fetch.ts, tests/fetch.test.ts</files>
  <behavior>
    - Test: fetchSource with hosts-format URL returns parsed domain array (mock fetch returning "0.0.0.0 malware.test\n")
    - Test: fetchSource with domains-format URL returns parsed domain array (mock fetch returning "malware.test\n")
    - Test: fetchSource throws on non-ok response (e.g., 503)
    - Test: fetchSource passes AbortSignal to fetch call
    - Test: fetchAllSources returns array of { index: Set, sourceId: string } for successful sources
    - Test: fetchAllSources logs warning and continues when one source fails (mixed success/failure)
    - Test: fetchAllSources returns empty array when all sources fail
    - Test: fetchAllSources logs warning for each failed source with source ID in message
  </behavior>
  <action>
1. Create tests/fetch.test.ts first (RED):
   - Mock `globalThis.fetch` with `vi.fn()` in beforeEach, restore in afterEach
   - Helper: `mockFetchResponse(body: string, ok?: boolean, status?: number)` that mocks a single fetch response
   - Test each behavior listed above

2. Create src/fetch.ts (GREEN):
   - `fetchSource(source: BlocklistSource, signal: AbortSignal): Promise<string[]>` -- fetches URL with signal combined via `AbortSignal.any([signal, AbortSignal.timeout(30_000)])`, checks response.ok (throw if not), calls response.text(), routes to parseHostsFormat or parseDomainList based on source.format
   - `fetchAllSources(sources: BlocklistSource[], signal: AbortSignal, log: (level: 'warn' | 'error', message: string) => void): Promise<Array<{ index: Set<string>; sourceId: string }>>` -- uses Promise.allSettled to fetch all sources in parallel, calls buildDomainIndex on each successful result, logs warning for failures with source ID and error message, returns only successful entries
   - Import parseHostsFormat, parseDomainList from './parse.js' and buildDomainIndex from './match.js'

Run all tests to ensure no regressions: `npx vitest run`
  </action>
  <verify>
    <automated>npx vitest run tests/fetch.test.ts -x && npx vitest run</automated>
  </verify>
  <done>fetchSource and fetchAllSources work correctly. Failed sources log warnings and don't prevent other sources from loading. All tests pass including Phase 1 tests.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass (Phase 1 + new fetch/preset tests)
- src/presets.ts exports two BlocklistSource constants
- src/fetch.ts exports fetchSource and fetchAllSources
- src/types.ts has log callback on FirewallConfig
</verification>

<success_criteria>
- PRESET_STEVENBLACK_UNIFIED and PRESET_HAGEZI_LIGHT are exported with correct URLs and formats
- fetchSource fetches a URL, parses content by format, returns domain array
- fetchAllSources handles mixed success/failure with warning logs, returns successful entries only
- FirewallConfig accepts optional log callback
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-lifecycle-and-configuration/02-01-SUMMARY.md`
</output>
